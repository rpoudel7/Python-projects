- This program takes a simple excel file as a input and calculates the changes in one column to the other column. 
- The file is in xlsx. 
- Useful in changing simple values in columns with several rows. 